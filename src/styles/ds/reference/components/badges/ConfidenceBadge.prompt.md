The confidence tag that rides next to every sourced number — the brand's "every number has a receipt" promise.

```jsx
<ConfidenceBadge confidence="high" source="World Bank" asOf="2024" />
<ConfidenceBadge confidence="low" />
```

`high`→green, `med`→sky, `low`→rose. Pass `source` / `asOf` and they surface on hover. Tiny (9px), uppercase, ultra-wide.
