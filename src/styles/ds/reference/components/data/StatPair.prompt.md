A mono label + tabular value, baseline-aligned — the atom of header stat rails.

```jsx
<StatPair label="requests" value={42} tone="accent" />
<StatPair label="problems" value={18} tone="violet" />
```

Stack several in a column for a PageHeader-style readout. `tone` colors the value; `size` scales it (sm/md/lg).
