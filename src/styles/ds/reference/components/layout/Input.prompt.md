A sharp-cornered text field on the canvas surface; the hairline border warms to the accent on focus.

```jsx
<Input type="email" placeholder="you@example.com" value={email} onChange={e => setEmail(e.target.value)} />
```

Pairs with `<Button variant="primary">` in the email-capture row. Sizes `sm | md`.
